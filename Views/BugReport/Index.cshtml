@model IEnumerable<StudentHub.Models.BugReport>
@using StudentHub.Models
@{
    ViewData["Title"] = "Prijave grešaka i prijedloga";
    var aspNetUserId = User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
    bool isSluzba = User.IsInRole("Studentska služba");
}

<div class="d-flex align-items-center justify-content-between mb-3">
    <h2 class="mb-0">@ViewData["Title"]</h2>
    <a class="btn btn-primary shadow-sm" asp-action="Create" title="Nova prijava">
        <i class="fas fa-plus me-2"></i> Nova prijava
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>Tip</th>
                        <th>Naslov</th>
                        <th>Datum</th>
                        <th>Status</th>
                        <th>Korisnik</th>
                        <th class="text-center">Akcije</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var prijava in Model)
                    {
                        <tr>
                            <td>
                                @if (prijava.Tip == BugTip.Bug)
                                {
                                    <span class="badge bg-danger">Greška</span>
                                }
                                else
                                {
                                    <span class="badge bg-info text-dark">Prijedlog</span>
                                }
                            </td>
                            <td>@prijava.Naslov</td>
                            <td>@prijava.DatumPrijave.ToString("dd.MM.yyyy HH:mm")</td>
                            <td>
                                @switch (prijava.Status)
                                {
                                    case BugStatus.Podnesen:
                                        <span class="badge bg-warning text-dark">Podnesen</span>
                                        break;
                                    case BugStatus.UObradi:
                                        <span class="badge bg-info text-dark">U obradi</span>
                                        break;
                                    case BugStatus.Zatvoren:
                                        <span class="badge bg-success">Zatvoren</span>
                                        break;
                                }
                            </td>
                            <td>
                                @($"{prijava.Korisnik?.Ime} {prijava.Korisnik?.Prezime}")
                            </td>
                            <td class="text-center">
                                <a class="btn btn-sm btn-outline-primary me-1" asp-action="Details" asp-route-id="@prijava.Id" title="Detalji">
                                    <i class="fas fa-eye"></i>
                                </a>
                                @if (!isSluzba && prijava.Korisnik.AspNetUserId == aspNetUserId && prijava.Status == BugStatus.Podnesen)
                                {
                                    <a class="btn btn-sm btn-outline-warning me-1" asp-action="Edit" asp-route-id="@prijava.Id" title="Uredi">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                }
                                @if (isSluzba)
                                {
                                    if (prijava.Status == BugStatus.UObradi)
                                    {
                                        <a class="btn btn-sm btn-outline-secondary me-1" asp-action="Odgovori" asp-route-id="@prijava.Id" title="Odgovori">
                                            <i class="fas fa-reply"></i>
                                        </a>
                                    }
                                    else if (prijava.Status == BugStatus.Zatvoren)
                                    {
                                        <a class="btn btn-sm btn-outline-info me-1" asp-action="EditOdgovor" asp-route-id="@prijava.Id" title="Uredi odgovor">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    }
                                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@prijava.Id" title="Obriši">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
