@model StudentHub.Models.Dokument

@{
    ViewData["Title"] = "Detalji dokumenta";
}

<h2>@ViewData["Title"]</h2>

<div class="card mt-3">
    <div class="card-header">
        <h4>Podaci o dokumentu</h4>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Student</dt>
            <dd class="col-sm-9">@Model.Student.Ime @Model.Student.Prezime</dd>

            <dt class="col-sm-3">Broj indeksa</dt>
            <dd class="col-sm-9">@Model.Student.BrojIndeksa</dd>

            <dt class="col-sm-3">Studentska služba</dt>
            <dd class="col-sm-9">@Model.StudentskaSluzba.Ime @Model.StudentskaSluzba.Prezime</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Naziv)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Naziv)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Putanja)</dt>
            <dd class="col-sm-9">
                @if (!string.IsNullOrEmpty(Model.Putanja))
                {
                    <a href="@Url.Content("~/uploads/" + Model.Putanja)" target="_blank">@Model.Putanja</a>
                }
                else
                {
                    <span>Nema datoteke</span>
                }
            </dd>
        </dl>
    </div>
</div>

<div class="mt-3">
    @if (User.IsInRole("Studentska služba"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Izmjena</a>
    }
    <a asp-action="Index" class="btn btn-secondary">Nazad na listu</a>
</div>
