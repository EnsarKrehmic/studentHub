@model IEnumerable<StudentHub.ViewModels.ObavjestenjeViewModel>

@{
    ViewData["Title"] = "Lista obavještenja";
}

<h2>@ViewData["Title"]</h2>

<div class="row mb-3">
    <div class="col-md-4">
        <form method="get">
            <div class="form-group">
                <label for="StudijskiProgramId">Odaberite studijski program</label>
                <select class="form-control" id="StudijskiProgramId" name="studijskiProgramId" onchange="this.form.submit()">
                    <option value="">Svi studijski programi</option>
                    @foreach (var program in ViewBag.StudijskiProgrami as IEnumerable<SelectListItem>)
                    {
                        var isSelected = program.Value == Context.Request.Query["studijskiProgramId"] ? "selected" : "";
                        @: <option value="@program.Value" @isSelected>@program.Text</option>
                    }
                </select>
            </div>
        </form>
    </div>
</div>

<p>
    @if (User.IsInRole("Profesor") || User.IsInRole("Asistent") || User.IsInRole("Studentska služba"))
    {
        <a asp-action="Create" class="btn btn-primary">Kreiraj novo obavještenje</a>
    }
</p>


@if (Model != null && Model.Any())
{
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Naslov)</th>
                <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Sadrzaj)</th>
                <th>@Html.DisplayNameFor(model => model.FirstOrDefault().DatumObjave)</th>
                <th>Studijski program</th>
                @if (User.IsInRole("Student") || User.IsInRole("Studentska služba") || User.IsInRole("Profesor") || User.IsInRole("Asistent"))
                {
                    <th>Akcije</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Naslov)</td>
                    <td>@Html.DisplayFor(modelItem => item.Sadrzaj)</td>
                    <td>@Html.DisplayFor(modelItem => item.DatumObjave)</td>
                    <td>@string.Join(", ", item.StudijskiProgramNazivi)</td>
                    <td>
                    @if (User.IsInRole("Student") || User.IsInRole("Studentska služba") || User.IsInRole("Profesor") || User.IsInRole("Asistent"))
                    {
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Detalji</a>
                    }
                    @if (User.IsInRole("Studentska služba") || User.IsInRole("Profesor") || User.IsInRole("Asistent"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Izmjena</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Brisanje</a>
                    }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Nema obavještenja za prikaz.</p>
}

<div>
    <a href="@Url.Action("Index", "Home")" class="btn btn-secondary">Povratak na početnu stranicu</a>
</div>