@model StudentHub.ViewModels.OcjenaViewModel

<div class="col-md-4 mb-4">
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white">
            <h5 class="mb-0">
                <a asp-controller="Predmeti" asp-action="Details" asp-route-id="@Model.PredmetId" class="text-white text-decoration-none">
                    @Model.PredmetNaziv
                </a>
            </h5>
        </div>
        <div class="card-body">
            <p><strong>Student:</strong> @Model.StudentIme @Model.StudentPrezime</p>
            <p><strong>Profesor:</strong> @Model.ProfesorTitula @Model.ProfesorIme @Model.ProfesorPrezime</p>
            <p>
                <strong>Ocjena:</strong>
                <span class="badge @(Model.Vrijednost >= 8 ? "bg-success" : Model.Vrijednost >= 6 ? "bg-warning" : "bg-danger")">
                    @Model.Vrijednost
                </span>
            </p>

            @if (User.IsInRole("Student") && Model.Vrijednost < 6)
            {
                <p class="text-danger"><strong>Napomena:</strong> Ova ocjena je ispod prolaznog praga!</p>
            }

            @if (User.IsInRole("Studentska služba"))
            {
                <p><strong>Prosjek predmeta:</strong> @Model.ProsjekPoPredmetu.ToString("0.00")</p>
            }

            @if (User.IsInRole("Student") || User.IsInRole("Profesor") || User.IsInRole("Studentska služba"))
            {
                <p>
                    <strong>Studijski program:</strong>
                    <a asp-controller="StudijskiProgrami" asp-action="Details" asp-route-id="@Model.StudijskiProgramId" class="text-decoration-none">
                        @Model.StudentStudijskiProgramNaziv
                    </a>
                </p>
            }

            <div class="text-end">
                <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-info btn-sm">Detalji</a>

                @if (User.IsInRole("Profesor"))
                {
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-sm">Izmjena</a>
                    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger btn-sm">Brisanje</a>
                }
            </div>
        </div>
    </div>
</div>
