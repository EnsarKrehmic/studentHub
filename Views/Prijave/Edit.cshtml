@model StudentHub.Models.Prijava
@{
    ViewData["Title"] = "Uredi prijavu";
    Layout = "_Layout";
}

<div class="container py-5">
    <div class="card shadow-sm mx-auto" style="max-width: 500px;">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0"><i class="fas fa-edit"></i> @ViewData["Title"]</h4>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post" novalidate>
                @Html.AntiForgeryToken()
                <div asp-validation-summary="All" class="alert alert-danger"></div>

                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="DatumPrijave" class="form-label"></label>
                    <input asp-for="DatumPrijave" type="date" class="form-control" />
                    <span asp-validation-for="DatumPrijave" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="IspitId" class="form-label"></label>
                    <select asp-for="IspitId" class="form-select" asp-items="ViewBag.IspitId">
                        <option value="">-- Odaberite ispit --</option>
                    </select>
                    <span asp-validation-for="IspitId" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="StudentId" class="form-label"></label>
                    <select asp-for="StudentId" class="form-select" asp-items="ViewBag.StudentId">
                        <option value="">-- Odaberite studenta --</option>
                    </select>
                    <span asp-validation-for="StudentId" class="text-danger small"></span>
                </div>

                <div class="d-flex gap-2 mt-4">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save"></i> Spremi izmjene
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Nazad
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
