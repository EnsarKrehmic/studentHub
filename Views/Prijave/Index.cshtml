@model IEnumerable<StudentHub.Models.Prijava>

@{
    ViewData["Title"] = "Lista prijava";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().datumPrijave)</th>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Ispit)</th>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Student)</th>
            <th>Akcije</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.datumPrijave)</td>
                <td>@Html.DisplayFor(modelItem => item.Ispit.Id)</td>
                <td>@Html.DisplayFor(modelItem => item.Student.Id)</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">Detalji</a> |
                    <a asp-action="Edit" asp-route-id="@item.Id">Izmjena</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Brisanje</a>
                </td>
            </tr>
        }
    </tbody>
</table>
