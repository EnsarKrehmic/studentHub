@model StudentHub.Models.StudijskiProgram

@{
    ViewData["Title"] = "Brisanje nastavnog plana";
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <h4 class="alert-heading">Da li ste sigurni da želite obrisati ovaj nastavni plan?</h4>
    <p>Ova akcija je nepovratna i trajno će ukloniti sve podatke o nastavnom planu.</p>
</div>

<div class="card mb-4">
    <div class="card-header bg-danger text-white">
        <h5>Podaci o nastavnom planu</h5>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Naziv:</dt>
            <dd class="col-sm-9">@Model.Naziv</dd>

            <dt class="col-sm-3">Opis:</dt>
            <dd class="col-sm-9">@Model.Opis</dd>

            <dt class="col-sm-3">Godina trajanja:</dt>
            <dd class="col-sm-9">@Model.TrajanjeUGodinama</dd>
        </dl>
    </div>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <div class="form-group">
        <button type="submit" class="btn btn-danger">Obriši</button>
        <a asp-action="Index" class="btn btn-secondary">Odustani</a>
    </div>
</form>